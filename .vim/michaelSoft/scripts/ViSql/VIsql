mysql components

globals
  vars
    host
    username
    password
    previousDB
  components
    findPrevPosition(keyword)

random components
  getCredentials()
  goToInterface()

database credentials view
  data needed
    database to see
    previous database seen
  entering
    draw the view
    set the binds
  leaving
  actions
    create a database
    select a database
    update/edit database
    delete a database
    sort database
  components
    drawDBView(database, sortOrder)
      drawDBHeader()
      drawDBContent()
    setDbViewBinds()
    selectDatabase(database)
    createDatabase()
    editDatabase(database)
    deleteDatabase(database)
  binds

database view
  data needed
  entering
  leaving
  actions
  components

tables view
  data needed
  entering
  leaving
  actions
  components

table data view
  data needed
  entering
  leaving
  actions
  components

table structure view
  data needed
  entering
  leaving
  actions
  components

table relationships
  data needed
  entering
  leaving
  actions
  components
